<Window x:Class="SimpleBinaryVCS.View.OverlapFileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleBinaryVCS.View"
        mc:Ignorable="d"
        Title="OverlapFileWindow" Height="450" Width="800"
        MinHeight="450"
        MinWidth="800"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="1" 
                 
                 
                 ItemsSource="{Binding Overlap}" SelectionChanged="ListBox_SelectionChanged"/>
        <Button Grid.Row="2"
            Content="Confirm"
                HorizontalAlignment="Right"
    Command="{Binding DataContext.FileTrackVM.AddForRestore, 
       RelativeSource = {RelativeSource AncestorType={x:Type Window}}}"/>
    </Grid>
</Window>
